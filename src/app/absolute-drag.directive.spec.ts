import { AbsoluteDragDirective } from './absolute-drag.directive';

describe('AbsoluteDragDirective', () => {
  it('should create an instance', () => {
    const directive = new AbsoluteDragDirective();
    expect(directive).toBeTruthy();
  });
});
